{% extends 'base.twig' %}

{% block content %}

    page

    {% if post.blocks %}{% include '_blocks/blocks.twig' %}{% endif %}

    <div class="row articles">

        {% for post in posts %}
            {% include 'partials/post.twig' %}
        {% endfor %}

    </div>

{% endblock %}